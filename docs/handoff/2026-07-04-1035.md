# Handoff — 2026-07-04 10:35

Bastão para o próximo Claude Fable, começando do zero. Este handoff **complementa** o anterior
(`docs/handoff/2026-07-03-2207.md`) — leia AQUELE primeiro para o contexto completo do projeto,
das regras de trabalho e do mapa de levers; depois leia este para o delta da sessão de 2026-07-04.
Não repito aqui o que o anterior já cobre.

---

## 0. Onde estamos (30 segundos)

O controle de combate está resolvido end-to-end (LT5, ver handoff anterior §3). Nesta sessão a
**construção do DCL avançou até o limite do offline**: o pipeline de dano same-hit está
construído, revisado, smoke-testado e commitado, com dois perfis de live test prontos. **O
bloqueio atual é 100% user-gated**: os live tests LT6/LT7 na máquina Windows. NÃO empilhe a
próxima camada (hit%/RNG) antes do LT6 passar — é a lição das 4 iterações do LT5.

Máquina desta sessão: **Linux (Raspberry Pi)** — tudo compila e testa aqui
(`RELOADEDIIMODS=/tmp/qualquer dotnet build/run`); só deploy + live test precisam do Windows.

## 1. O que foi feito nesta sessão (2026-07-04)

Três commits (push **NÃO** feito — gated pelo usuário; `f0b20a6..26ff646` estão só locais):

1. **`c6256f8`** (sessão anterior, já estava committed): `AbilityCatalog` — CSV WotL de 512
   abilities → vars de fórmula (`ability.x/y/element_*/inflict_*...`) + builds Linux.

2. **`9461f39` — o slice do pipeline DCL (`DclPipelineEnabled`)**, o coração desta sessão.
   Switch único que liga: probe calc-entry `0x309A44` → `DclActionContextCache` (contexto de ação
   por alvo: casterIdx/actionType/abilityId) → callback gerenciado no pre-clamp `0x30A66F` monta o
   `FormulaContext` completo (atacante+alvo+equipamento via `ItemCatalog`+ability via
   `AbilityCatalog`) e reescreve o debit staged com `DclDamageFormula`, same-hit. Falha em
   qualquer ponto → fall-through para o vanilla + `[DCL-MISS]`/`[DCL-ERR]` no log. Arquivos
   novos: `DclActionContextCache.cs`, `FormulaRuntimeContextBuilder.cs`.
   **Revisão independente (gpt-5.5 via `mcp__codex__codex`, read-only) aplicada**: 1 BLOCKER real
   corrigido — o stub asm publicava o count do ring ANTES de escrever o slot (janela de slot
   rasgado); agora escreve o slot primeiro e publica por último. Validator ganhou guards: DCL +
   plan/forced-debit/credit = ERRO de deploy (escrevem depois do callback e sobrescreveriam o
   resultado em silêncio); `CalcEntryProbeRva` obrigatório; warn em `LogOnly`.

3. **`26ff646` — `DclDerivedVariables`**: cadeias multi-passo de fórmula avaliadas em ordem no
   contexto DCL antes da `DclDamageFormula` (mesmo schema de `FormulaDerivedVariables`; o engine
   agora delega para o helper compartilhado). Input desconhecido em qualquer elo = erro de deploy
   (o validator caminha a cadeia num contexto sintético) e `[DCL-ERR]` em runtime.

Docs atualizados (leia): `docs/modding/06-code-mod-runtime-dsl.md` §"DCL pre-clamp pipeline
(`DclPipelineEnabled`)" — a spec completa de settings/vars/semântica — e o parágrafo de status
2026-07-04 no topo de `docs/modding/08-dcl-information-requirements.md`.

## 2. Os dois perfis de live test (prontos, validados com 0 erros)

- **`work/battle-runtime-settings.lt6-dcl-preclamp.json` — LT6, rodar PRIMEIRO.** Slice mínimo de
  encanamento: stack força-hit do LT5-A4 + fórmula previsível
  (`a.pa * 2 + aslot.weapon.weaponPower + ability.y`). PASS = todo golpe conecta E o HP que cai
  bate com o `debit` das linhas `[DCL]` do `battleprobe_log.txt`, diferente do `oldDebit`.
  `[DCL-MISS]`/`[DCL-ERR]` = investigar antes de prosseguir.
- **`work/battle-runtime-settings.lt7-dcl-damage-model.json` — LT7, depois do LT6.** O modelo de
  dano GURPS provisório portado do perfil reconciler antigo (`dcl-damage-slice`) para entrega
  same-hit via `DclDerivedVariables`: base thr/sw por `a.rawPa+4`, + Power da arma, − DR
  subtrativo por classe de armadura × tipo de dano, × wound multiplier, × trait de Brave
  (760–1350‰). **Só basic attacks** (`action.type == 1`); magias ficam vanilla. NÚMEROS
  PROVISÓRIOS — calibração explicitamente adiada por Marcelo.

Ciclo de teste (regras completas no handoff anterior §2): usuário fecha o jogo → deploy
`build-deploy.ps1 -RuntimeSettings <perfil> -AllowReloadedOpen` no Windows → usuário liga só
modloader+codemod na GUI (data mod OFF) → batalha, ~6 ataques básicos → reportar log.
Ambos os CSVs (`item_catalog.csv`, `wotl_ability_action_baseline.csv`) deployam sozinhos via
csproj Content — nada manual.

## 3. Próximos passos (em ordem)

1. **LT6 na máquina Windows** (checklist acima). Se falhar: os logs `[DCL-*]` dizem onde —
   cache-miss (probe não fira/ordem), erro de leitura de unit, erro de fórmula.
2. **LT7** (modelo de dano). PASS = dano físico segue o modelo (varia com PA/Brave/arma/armadura
   do alvo), magia intocada.
3. **Hit% + RNG próprio + forçamento binário** (handoff anterior §6.3c): o mod computa a % DCL,
   rola o próprio RNG e força o resultado via levers provados (ItemTableEvadeZero + class `+0x4B`
   + Brave). SÓ DEPOIS do LT6/LT7 validarem a entrega.
4. **Calibração dos números do modelo de dano COM Marcelo** (MUST-AUTHOR, handoff anterior §6.4).
5. Opcional: LT5-C (miss/parry/block visuais num hit), ver handoff anterior §6.5.

## 4. Gotchas novos desta sessão (além dos do handoff anterior §8)

- Neste Linux, o build exige `RELOADEDIIMODS` setado (qualquer path serve p/ compile-check):
  `RELOADEDIIMODS=/tmp/claude-1000/reloaded-mods dotnet build ...`.
- Validator de perfis roda offline:
  `dotnet run --project codemod/fftivc.generic.chronicle.codemod.settingsvalidate -c Release -- <perfil.json>`
  (o build-deploy.ps1 também roda ele automaticamente no deploy).
- O perfil `work/battle-runtime-settings.dcl-damage-slice.json` é a versão RECONCILER (pós-hit,
  DryRun) do modelo — histórico, não use para o pipeline novo; o LT7 é o sucessor.
- Os 4 warnings do validator nos perfis LT6/LT7 são esperados (stack força-hit + callback
  gerenciado); 0 erros é o gate.
- A memória `dcl-codemod-construction-state` (desta máquina) tem o resumo do estado; as memórias
  citadas no handoff anterior (`miss-block-parry-control` etc.) vivem na máquina Windows.

## 5. Skills sugeridas para a próxima sessão

- **`mcp__codex__codex`** (MCP, não é skill mas é o canal): revisão independente de qualquer
  código novo antes de milestone-commit (padrão desta sessão; `sandbox: read-only`).
- **`/loop`**: foi como esta sessão rodou ("leia o último handoff e trabalhe até o fim") — bom
  para retomar autonomamente.
- **`/handoff`**: gere o próximo bastão ao fechar a sessão.
- Regras de divisão de trabalho, deploy e commit: handoff anterior §2 (continua tudo válido).

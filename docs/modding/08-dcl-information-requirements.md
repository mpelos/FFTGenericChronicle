# DCL runtime information requirements

This document is the canonical inventory of information the code mod must own to implement the
Deep Combat Layer (DCL). It maps each requirement to its authoritative document and confidence; it
does not duplicate field maps, hook ABIs, rule schemas, design rules, or investigation history.

The ownership boundary is:

- `03-battle-data-map.md` — editable data tables, enums, and catalog sources;
- `04-engine-memory-model.md` — live fields, unit records, actor/action state, and lifecycle;
- `05-reverse-engineering.md` — hook anchors, native/virtualized boundaries, and control surfaces;
- `06-code-mod-runtime-dsl.md` — runtime settings, formula variables, rule schemas, and transactions;
- `docs/deep-combat-layer/` — combat design and policy.

Confidence labels are **Proven**, **Strong**, **Hypothesis**, and **Refuted**. “Implemented offline”
describes code coverage, not engine confidence. A mechanism remains live-gated wherever its native
ordering or presentation has not been observed end to end.

## 1. Action identity and lifecycle

| Requirement | Required information | Coverage and owner |
| --- | --- | --- |
| Acting unit | Stable caster/attacker identity for every formula and effect. | **Proven** for ordinary, delayed, native-repeat, and accepted Reaction transactions. `04` owns attribution. CT is **Refuted** as authority. |
| Action identity | Action type, ability id, payload, and outer/nested provenance. | **Proven** at calculation entry and outer-sweep result boundaries. Formula `0x25` nested Attack identity is explicitly separated from its outer owner. See `04` and `06`. |
| Target identity | Unit or tile target, final impacted units, and AoE iteration. | **Proven** for final result targets; **Strong** for all pre-resolution shapes. See `03`, `04`, and `06`. |
| Resolution phase | Forecast, AI evaluation, confirmation, charge, execution, apply, and cleanup. | **Proven** for numeric forecast/evaluation/execution separation. Status, Reaction, and movement families carry their own confidence in `04` and `06`. |
| Native repeat | Repeat count/index and active hand/item for each native strike. | **Proven** for weapon Attack and Reaction-delivered Dual Wield. Other repeat families require explicit policy. See `04`. |
| Managed multistrike | Explicit strike list, per-strike contest, damage, and atomic resource commit. | **Implemented offline**. See `06`. |
| Reaction source | Exact equipped owner, incoming source/target result, normalized effect source/target binding, native delivery carrier, commit, and delivered strike. | **Proven** for the supported synthetic Counter carrier. Canonical ReactorToSource, ReactorToTarget, SourceToReactor, and Explicit routes are implemented offline and generation-safe; broader native families retain individual confidence. See `04` and `06`. |
| Pending action | Owner, action identity, timer, Charging mirrors, and cancellation lifecycle. | **Proven** for the Interrupt transaction and later normal recovery. See `04` and `06`. |
| Native side effects | HP/MP channels, status packet, result kind/flags, inventory effects, and special consumers. | **Partial**. Each owned effect requires an explicit producer; `+0x1A8/+0x1D0` are not generic status authority. See `04` and `06`. |

## 2. Unit and battle state

| Requirement | Required information | Coverage and owner |
| --- | --- | --- |
| Unit registry | Pointer, table index, character identity, generation, and removal. | **Proven/implemented**. `04` owns the array; `06` owns generation-aware caches. |
| Core stats | HP, MP, PA, MA, Speed, Brave, Faith, CT, Move, Jump, level, job, zodiac, and gender flags. | **Proven** for the live fields used by formulas. See `04`. |
| Raw/effective stats | Base values versus equipment/status-modified values. | **Proven** for PA, MA, and Speed. See `04`. |
| Selected-unit DCL information | DCL status-screen values, component breakdowns, pool terms, encumbrance, and Critical mobility adjustment. | **Implemented offline**. One projection consumes the synchronized native unit projection, normalized equipment snapshot, explicit non-equipment inputs, raw PA/Speed/MA, and current Brave, then exposes ST/DX/IQ components, HT, Faith, HP/MP current/base/final terms, Basic Lift/Speed, Move/Jump/Dodge, exact encumbrance, and Critical final Move/Dodge without reconstructing mechanics from native formula ids, item names, or draft job assumptions. Native selected-unit UI binding remains live-gated. See `06`. |
| Progression | Job id, spendable JP, total JP, job level, EXP, and family skill grade. | JP arrays and threshold derivation are **Proven**; remaining packed job-level candidates retain their confidence in `04`. Job-family grades remain authored DCL policy. |
| Turn owner | Exact own-turn edge for refreshes and durations. | **Proven** through active-turn state. See `04`; `06` owns resource refresh behavior. |
| Status state | Five-byte source, immunity, effective, and durable-master arrays. | **Proven**. Direct add/remove and immunity input control are **Proven**. Engine-owned lifecycle statuses require dedicated mechanisms. See `04` and `06`. |
| Position/facing | X, Y, level, facing, committed destination, and accepted route. | **Proven**. See `04`. |
| Movement settlement | Final committed tile, route completion, and post-movement state. | Position and accepted-route fields are **Proven**. DCL gameplay does not interrupt movement between tiles; position-triggered mechanics begin only after movement finishes. See `04` and `06`. |
| Custom-state revision | One immutable target revision from planning through confirmed commit. | **Implemented offline**. Every confirmed target-batch transaction validates the current battle registry before callbacks; direct/Area/physical/auxiliary/ForcedMovement/system-command coordinators reject stale snapshots before RNG. Aim retention/cancellation is prospective until commit. See `06`. |
| Tile map | Dimensions, level, height, slope/depth, markings, flags, and occupancy. | **Proven** for the mapped table and common mark bits; unusual multi-level cases retain inline confidence in `04`. |
| Allegiance | Team, faction, friend/foe, and targeting legality. | **Proven** for the runtime signals used by DCL sanity checks. See `04`. |

## 3. Equipment and item metadata

The DCL reinterprets existing items; it does not add new equipment SKUs.

| Requirement | Required information | Coverage and owner |
| --- | --- | --- |
| Equipped slots | Main hand, off hand, shield, head, body, and accessory ids. | **Proven**. `04` owns live offsets. |
| Catalog resolution | Stable item-id to row and runtime metadata mapping. | **Strong/implemented** through `ItemCatalog`; data source is owned by `03`. |
| Active weapon | Exact native hand, side, item, family, and repeat provenance. | **Proven** for supported weapon transactions. See `04` and `06`. |
| Weapon family/reach | Family, native range, reach policy, projectile/line behavior, and unarmed route. | Native fields are mapped; DCL classification remains explicit catalog policy. See `03` and `06`. |
| Damage type/modifier | Cut, thrust, crush, missile, magic, power modifier, and special route. | Must be authored in DCL metadata because stock tables do not expose the complete vocabulary. |
| Parry/block | Native item values plus battle-owned repeated-Parry counters and one Block availability flag. | Native source tables are **Proven**. Cumulative `-4` Parry attempts, one Block between defender turns, revision-checked outer-action commit, own-turn reset, and checkpoint restore are **implemented offline**. Shield evade has no universal late unit-byte lever. See `05` and `06`. |
| Armor/DR | Armor class and type-specific subtractive DR. | Must be authored in DCL metadata. |
| Weight | Item Weight and its Move/Dodge policy. | Authored DCL metadata is **implemented offline**; no stock IVC item Weight field is mapped. Equipment information projection exposes only the item's authored visible properties, including weapon damage/readiness/ParryLoad, armor DR, shield coverage, focus terms, Weight, elements, immunities, and special properties. Equipment-change preview compares before/after selected-unit projections for Load, encumbrance band, next threshold, Move/Jump/Dodge, BodyDR/HeadDR, MaxHP/MaxMP, Magic Resistance, and focus modifiers. Native equipment-screen binding remains live-gated. See `06`. |
| Elements/affinities | Element, absorb, nullify, halve, weak, strong, and boost. | Source fields exist; runtime exposure is partial. See `03` and `06`. |
| Status grants/immunities | Innate, initial, and immunity sets. | Source fields exist; live status authority is owned by `04`. |
| Special behavior | Proc, drain, thrown, gun, rod/staff, random-power, and monster/fist routes. | Partial. Every special family requires explicit catalog and runtime policy. |

## 4. Ability and effect metadata

| Requirement | Required information | Coverage and owner |
| --- | --- | --- |
| Ability identity | Stable id/name and ability category. | Cataloged. See `03`. |
| Native action row | Formula, X/Y, element, range, area, vertical, status, CT, MP, and flags. | The override table is sparse. The WotL baseline is a design aid; IVC overrides and observed behavior remain authoritative. See `03` and `05`. |
| DCL effect kind | Damage, heal, status, movement, Reaction, Interrupt, instant KO, MP, or special. | Must be explicitly authored in `AbilityCatalog`/runtime rules. See `06`. |
| Power/scaling | Spell power, heal power, weapon scaling, Faith/Brave policy, and damage-type mapping. | Must be explicitly authored. DSL support exists. |
| Hit policy | Physical contest, Magic Evade, guaranteed outcome, status resistance, or special check. | Runtime authority is implemented for physical and magic binary outcomes; per-action selection remains metadata. See `05` and `06`. |
| Status nature and duration | Per-source physical-health/HT, mental-Will, beneficial, lifecycle, campaign, or explicit special policy plus a positive owned duration where applicable. | The DCL defines supported category families and keeps unresolved harmful-status natures as fail-closed design inputs. Ability ids `213/214` are explicit physical Stun/Knockdown candidates for the shared Disable/Immobilize native flags; other Disable/Immobilize producers still require authored effect nature before native counter transfer. Darkness, Silence, BloodSuck, Oil, Undead, Slow, Stop, Sleep, Petrify, Frog, and other unresolved harmful statuses have no selected generic resistance category. Faith is not inferred as a default resistance stat. Disease has no assigned native carrier or ongoing-effect mechanism. These are explicit design inputs, independent of job assignment. |
| Native rider policy | Absent, suppressed by data, retained carrier, or engine-owned. | Required and fail-closed for managed status rules. See `06`. |
| Targeting/AI policy | Ally/enemy/map filters, shape, scoring, and transient evaluation behavior. | Native flags and numeric AI writer are mapped. Authored status/heal/special scoring remains partial. See `03`, `04`, and `06`. |

## 5. Formula inputs and derived variables

| Requirement | Coverage and owner |
| --- | --- |
| Thrust/swing/base tables | Supported by formula tables/maps in `06`. |
| Armor DR and wound multipliers | Supported once item damage type and armor metadata are authored. |
| Penetration floor and clamps | Supported by the DSL; preview and execution must call the same policy. |
| Brave/Faith curves | Live values are **Proven**; authored curves live in settings. |
| Zodiac compatibility | Live identity is exposed; authored matrix lives in settings. |
| Weapon skill | Formula maps support it; job/family grade ownership remains required. The canonical skill-training contract is implemented offline: weapon/shield Difficulty, explicit job/family Aptitude Tier, native Job Level, controlling attribute, and explicit post-training modifiers resolve into one Rank, base score, and final score without reading draft job content. The synchronized unit projection retains JobId, JobIndex, spendable JP, total JP, and derived Job Level. An explicit `(JobId, SkillFamily) -> AptitudeTier` policy map resolves the selected weapon family and fails closed when a row is missing. The native physical composer can derive the retained training object from the synchronized source snapshot plus explicit policy inputs. Physical Strike evaluation and execution accept that object as the base weapon-skill source and fail closed if it disagrees with the Strike skill. |
| Deterministic contest rolls | Event/transaction identity and cached decisions provide preview/execution parity for owned families. |
| Native result context | HP/MP channels, active weapon, result kind/flags, pending state, and DCL state are exposed by the relevant runtime contexts. |

## 6. Outcome authority

| Requirement | Coverage and owner |
| --- | --- |
| Forecast hit percentage | **Proven/implemented** through the per-target cached percentage and native display-copy hook. See `05` and `06`. |
| Canonical forecast/AI family dispatch | **Implemented offline**. One atomically classified ability family selects the exact physical, direct/Touch, Area, status/removal, Dispel, Quick, Revive, or ForcedMovement evaluator and returns both player and AI projections without RNG. A retained composed evaluation wrapper carries ability id, family, and family input across forecast/AI call boundaries; the dispatcher re-resolves the runtime family and rejects a retained-family mismatch before evaluator or projection selection. Native forecast/AI composition consumes the same classified admission, synchronized snapshots, and explicit family-policy inputs as confirmed native composition, then projects only deterministic data into evaluation; ActionInstance ownership, Reaction candidates, sampled rolls, and state materialization side effects are excluded. A retained native composed plan can carry both execution and evaluation wrappers only when they share the same ability id and canonical family. Preserved native specials are explicit passthroughs. Native hook invocation and carrier-field writers remain gated. |
| Canonical confirmed-execution family dispatch | **Implemented offline**. The same classified family selects one resolve-and-publish coordinator from deterministic inputs. The battle owner retains all conditional RNG, state/resource commits, the published carrier, and one Reaction window; mismatched family, ability, or duplicate Reaction ownership fails before mutation. One classified native composition entry projects the completed action's frozen index-zero rows from exact unit-policy inputs, then accepts only the exact family-policy type for DirectNumeric, complete-sequence PhysicalDamage/AreaNumeric, StatusApplication/removal, Dispel, Quick, Revive, or standalone ForcedMovement. Shared single-target magic declaration policy materializes only common casting facts from one complete unit-targeted nonrepeat admitted action. DirectNumeric policy extends it with explicit defense, riders, Reactions, Touch route, state penalties, and authored movement branches. Auxiliary single-target policy extends it for StatusApplication, StatusRemoval, Quick, Revive, Dispel, and ForcedMovement while keeping state materialization, CT/QuickLock, Undead, Dispel selection, and final native movement verdict policy explicit. Physical and Area policy derive declared unit target or fixed tile from complete admitted sequences while keeping weapon/route/defense/Strike/protection/skill policy and per-target Area Dodge/resistance/status/movement/Injury policy explicit. One battle-scoped policy-source ledger stores a validated ticket per retained ActionInstance without RNG or native publication; its production intake returns no-write missing-admission/duplicate-ticket statuses, recognizes duplicates before candidate validation, and keeps invalid policy facts strict for new tickets. A strict schema-revisioned policy-ticket template loader can materialize DirectNumeric, PhysicalDamage, AreaNumeric, property-payload StatusApplication, StatusRemoval, Dispel, Quick, Revive, and standalone ForcedMovement tickets from explicit unit-height and family-policy facts, returns a no-write missing-template status, and rejects unsupported families, invalid template fields, invalid Dispel score, Quick CT outside the pre-turn range, negative Revive Faith, incomplete Revive Undead tables, unresolved/invalid ForcedMovement verdicts, or empty/invalid Physical/Area target or Strike policy before publication. The canonical runtime snapshot optionally carries that template registry; absent path means empty registry, present path is loaded all-or-nothing with authoring/items/bindings/reaction bindings. The retained-action bridge exposes no-write readiness gates, symmetrical ticket-side/admission-side handshakes, and an admission-side template producer path: either half can arrive first, a retained pair settles immediately through the classified family provider, and a loaded template can build the ticket after admission while a missing template leaves the admission retained without RNG; DirectNumeric and standalone ForcedMovement prove this producer path end-to-end. PhysicalDamage, AreaNumeric, property-payload StatusApplication, StatusRemoval, Dispel, Quick, Revive, and standalone ForcedMovement templates are validated through the ticket ledger without native carrier execution. An ActionInstance in the native ledger's published-or-retired identity set cannot be retained, republished, or resurrected. The dispatcher can consume the retained composed wrapper directly and revalidates its family against the runtime catalog before RNG or publication. PhysicalDamage composition accepts an explicit Cover/Bodyguard plan that preserves the declared target while routing the final target batch to the protector. Job/command, timeline, lifecycle-policy, and native-map verdict inputs stay explicit. Live hook proof and production policy-source capture for non-templated/native-rich facts remain gated. |
| Canonical outer-action admission | **Partially Proven live / implemented offline**. The native target-list builder finishes before `0x281EF7`, so one complete TargetBatch is available before any per-target calculation; the guarded live hook installs at `0x281EFA`, after `rbx` is initialized and before a target-index read. Because that anchor executes inside the 21-slot native loop, managed admission suppresses identical source/action/repeat/selected-target/TargetBatch keys inside a short duplicate window before allocating the sweep serial. The selected-unit byte at RVA `0x7B0792` and order X/map-level/Y tuple are retained separately before expansion; the complete TargetBatch is declared-target authority for unit-target composition, while selected-unit remains separate policy/diagnostic input. A battle sweep serial reserves one ActionInstance; exact state-`0x2A` index zero plus contiguous state-`0x2F` repeat indexes reuse it with normalized StrikeCount and immutable source/action/selected-tile/batch identity. The disabled-by-default guarded hook preserves native state, validates current source/target UnitKeys, ignores unbound native abilities, publishes only a complete nonrepeat or contiguous repeat sequence, and passes the complete captured outer action through the admission-side handshake. Duplicate ActionInstance publication, post-retirement replay, or row/identity drift fails closed. Source, selected-unit, target rows, and explicitly supplied auxiliary rows such as a Cover/Bodyguard protector are deep-copied at index zero, so later repeat sweeps cannot substitute already-mutated pools or states. DirectNumeric template production is live-proven for one ordinary Fire ActionInstance with `Published` admission, `Built` template, `Published` ticket, and `Published` retained bridge status. Snapshot projection requires one explicit unit-policy input per frozen row; the unit-policy provider validates that coverage, rejects duplicate UnitKeys and prepopulated equipment attribute channels, and does not infer job modifiers, tile height, eligibility, or defense keys. RandomFire fails capability loading until DCL owns the complete repeated target sequence. Production policy-source capture for non-templated/native-rich facts remains gated. |
| Forecast HP amount/bar | **Proven/implemented** through the underlying staged forecast fields. See `05` and `06`. |
| Runtime HP/MP amount | **Proven/implemented** at the normalized compute point and exact pre-clamp consumption. See `04` and `06`. |
| HP/MP ResourceChange and Drain | **Implemented offline** with explicit target/source/Undead routing, exact dice and forecast/AI correlation, independent target/source caps, KO cleanup, source-effect-before-payment ledger order, and transactional native pool writers. Area delivery advances each target in stable Strike order while sharing one capped source pool; exact player/AI evaluation retains the single caster-roll correlation and dynamically combines every target gate, magnitude, KO short-circuit, and source-cap branch. Player-facing ResourceChange projection names HP/MP, TargetCredit/TargetDebit/Drain, rolled range, both pool caps, target/source Undead routes, no-delivery, target/source KO, rejection, expected target/source debit or credit, and excess lost to each cap. Native projection separates every target carrier, one aggregate source effect, payment, and the terminal Reaction window. The final target readback plus nonlethal source-effect/payment epilogue is **Strong / static-proven** and disabled by default; live proof and a native lethal source-pool lifecycle carrier remain gated. |
| Healing/revive/inversion | Direct and delayed explicit healing are **Proven**; native result-kind gates prevent cancelled results from being resurrected. See `06`. |
| Physical miss/parry/block | **Implemented offline** with one contest per Strike, cumulative per-weapon/limb Parry attempts, one Block between defender turns, battle-owned revision-checked commit, and ordered physical Damage-plus-status-Rider resolution. Canonical admission retains the native item and hand for each weapon sweep. Forecast, AI, state projection, and confirmed execution resolve each Strike's own synchronized weapon metadata, damage expression/type, ranged route, skill input, readiness, and slot-bound state; captured and explicit hand identities must agree. A promoted lone left weapon remains left-slot-bound, while item zero uses the canonical unarmed/limb profile. The normal-Attack binding explicitly accepts one single-hand Strike or its two-Strike maximum; fixed multistrike carriers remain exact. Dual Wield remains one outer action with main-hand then off-hand Strike, one payment, and one post-sequence Reaction window. A landed Strike reuses its attack margin when the Rider authors QuickContest; None and SuccessRoll retain their distinct gates. Miss, defense, immunity, and KO short-circuit suppress unreachable Rider RNG. Representative native invocation and presentation remain live-gated. |
| Immediate combo reprojection | **Implemented offline** for physical and Area Damage: surviving Major-Wound HT branches apply Stun/Knocked Down penalties to later target-local defense gates in execution and exact forecast/AI without opening another Reaction window. Generic Immediate status Riders remain unsupported. |
| Injury concentration timeline | **Implemented offline** for physical, direct-magic, and Area Injury: confirmed execution validates the target Charging snapshot before RNG, resolves at most one incident per landed Strike across Injury and displacement, and cancels the exact pending action before Riders and the terminal Reaction window. Failed checks use one reachable concentration site; KO, Stun, and Knocked Down cancel directly without that roll. Native Charging-status and timeline callbacks remain live-gated. |
| Typed action-state projection | **Implemented offline** for Taunt legality and Guard Broken/slot-bound Weapon Bound/Bulwark physical terms. One revisioned physical state plan feeds player forecast, AI, and confirmed execution; a shared nonphysical planning gate suppresses the family evaluation/player/AI object under Taunt; confirmed direct/Area/auxiliary/ForcedMovement and Ready/Reequip/Stand Up semantic coordinators reject Taunt before RNG. Universal-command availability uses the same Taunt/action-resource boundary before UI/AI selection; Ready is per registered weapon, Reequip consumes an explicit equipment-policy verdict, and Stand Up requires both turn resources plus the current Knocked Down state. A source-owned native snapshot batch joins current UnitKeys, normalized equipment, exact state revision, finite defenses, pools, tile height, and explicitly supplied non-equipment characteristic inputs without inferring job content. Direct composition consumes that batch to derive Silence, Faith, focus, affinity, Reflect/Oil/Shell, state-aware DR, IQ magnitude, pools, and Injury context; job/command-policy inputs stay explicit. Direct, physical, and Area target-Aim removal is commit-bound. Weapon-delivered Reaction candidates carry an exact equipment slot, and Weapon Bound suppresses only the matching slot in confirmed and forecast/AI sets. Fear is excluded. Bulwark displacement/passability consumption awaits canonical semantics. Native command-list/targeting presentation and candidate-slot production remain gated. |
| State presentation channels | **Implemented offline**. The pure resolver separates above-unit status icons from timeline, equipment, defensive-panel, and selected-state detail facts. Ailment-like custom states can expose symbolic native unit icons, while QuickLock, Ready/Unready, spent Block, and repeated-Parry counters cannot fall back into the unit-icon layer. QuickLock uses the existing Haste icon only through the CT timeline. Channel snapshots are stable by state instance id, keep detail-only technical states explicit, and fail closed when a visible state lacks a display name. State presentation profiles validate symbolic asset references, display names, selected-unit detail, and state-to-profile catalog coverage, and the canonical runtime loads the profile bundle atomically with authoring, items, ability bindings, Reaction bindings, and optional policy-ticket templates. The unit-presentation snapshot consumes active state instances plus the loaded catalog to resolve body position, palette, invisibility layering, and all state-detail channels together; native KO and transformation keep priority, KO suppresses temporary palettes, and persisted presentation-id drift fails closed. Bundle validation requires every StatusApplication and StoredReraise action to declare the presentation profile of each persistent state it can materialize. A native-surface audit derives the exact pose, palette, transparency, above-unit icon, CT timeline icon, selected-state detail, equipment-detail, defensive-panel, command-disablement, and entry-feedback bindings required by a snapshot, accepts explicit live-gated rows, and rejects missing rows or proven rows without stable proof ids. Ready/Unready and spent-defense states remain detail-only while still requiring selected-equipment or defensive-panel native bindings. Native asset resolution and UI binding remain gated. |
| Magic Evade | **Implemented offline** with shared forecast/execution decision and native-repeat policy. Direct and standalone ForcedMovement External Projectile families validate authored Dodge/Block legality against the current battle defense snapshot, spend the one-per-turn Block on each reachable attempt, and expose the same exact spend probability to forecast/AI. Live candidate construction and apply/presentation ordering remain family-gated. |
| Touch delivery | **Implemented offline** for the exact SpellScore attack gate, authored Dodge/Parry-only policy, critical bypass, cumulative per-weapon/limb Parry spend, later-Strike defense reselection, immutable native-route verdict, shared Damage/Healing/ResourceChange and Rider composition, payment, Injury/Aim state, revisioned target transaction, one Reaction window, native projection, and exact player/AI evaluation. Native route/candidate snapshot production and representative live application remain gated. |
| Status application | **Implemented offline / partially live-proven** through owned packets, immunity, resistance, and carrier policy. Generic Riders derive None, SuccessRoll, or QuickContest from the referenced state definition; immunity and an absent Carrier precede every gate, and Explicit requires a named owner. Direct, Touch, Area, and physical status Riders retain their exact state instance through commit and native projection; the physical path resolves once per landed Strike. A pure Area status Carrier uses its Area delivery gate exactly once: QuickContest shares the one per-target contest, None/Dodge require a no-resistance state, immunity suppresses delivery before RNG, and native publication carries empty numeric channels plus the exact state instance. Direct/Touch and Area player/AI plans expose exact correlated application probabilities. Atomic runtime classification requires standalone and pure-Area status Carriers to agree with their referenced state gate, so action and state cannot diverge silently. Native counter neutralization remains fail-closed unless every producer for the selected timed status has an explicit nature, resistance, and owned duration policy; shared Disable/Immobilize flags currently retain unresolved nonphysical producers and therefore are not exclusive-runtime-owned. Performance and random-repeat cadence retain explicit gates. |
| Reaction triggering | Exact native and VM-owned trigger routes are partially **Proven**; supported synthetic delivery is **Proven**. Confirmed physical, direct/Touch/Area magic, status application/removal, Dispel, Quick, Revive, and standalone ForcedMovement coordinators resolve and retain the one post-action window; ResourceFailure and the later Stored-Reraise lifecycle trigger do not create one. Native publication rejects a declared window without its exact same-ActionInstance result, including an empty accepted set. A strict native binding separates Reaction presentation/dispatch id from executable effect ability and exact effect Action revision. Canonical `ActivationRoll` references are a closed DX/HT/IQ/Will/named-Skill set and reject unknown references before execution or forecast; raw Brave is not a canonical Reaction chance source. Reaction player forecast and AI projection share the same ordered RNG-free evaluation; player rows expose activation mode, chance, natural-effect-gate flag, blocking reason, effect Action id, and exact effect source/target, while AI consumes the same exact activation masses. The state-`0x2C` per-effect-Strike acknowledgement and one-per-outer-action terminal empty-queue boundary are implemented behind disabled live gates; terminal settlement fails closed until every accepted effect Strike completes. See `04` and `06`. |
| Final-tile triggers | **Implemented offline / live-gated**. DCL position-triggered abilities evaluate only after an accepted ordinary-movement finalizer snapshot. The battle validates the current mover identity and final tile, resolves unique prepared candidates in stable state-instance order, rejects replayed movement sequences, and consumes the next declaration-time Action reservation for each firing Overwatch state. Triggered Actions enter a battle-owned queue; only its head can publish through the confirmed dispatcher and it blocks later movement, timeline advancement, and checkpoint capture until exact native settlement. Mid-route interruption is excluded from gameplay. Native finalizer invocation and carrier application remain gated. |
| Forced movement carrier | **Implemented offline** for normalized standalone and pure-Area ForcedMovement: explicit distance/direction, one immutable native map verdict per target shared by forecast/AI/confirmed execution, Carrier immunity before resistance RNG, exact finite Block spend where applicable, battle-owned defense-resource commit, origin/final-destination readback, empty numeric channels, and one post-action Reaction window with no per-tile event. Player forecast exposes authored direction/distance, native origin/destination, actual tiles moved, fall, delivery probability, expected moved tiles, and concentration-interruption probability from that same verdict. Positive displacement removes the exact target Aim instance inside the guarded outer commit and projects that same removal beside the movement carrier. It also resolves one concentration incident against the exact pending charged action and commits failure before Reaction; resistance, immunity, and zero displacement create no incident. Injury critical/authored displacement is **Implemented offline** for physical, direct magic, and Area execution/projection, with exact physical/direct/Area forecast and AI moved-tile/fall outcomes. KO and blocked-zero branches suppress Aim/concentration consequences. Multi-Strike physical and Area actions use per-Strike branch forests keyed by every reachable origin; the movement-only tile state does not alter deferred combat gates. Native publication requires a contiguous origin/destination chain and native apply validates every selected movement by readback in Strike order. Native map, timeline, and Charging-status callbacks remain live-gated. See `06`. |
| Charged-action Interrupt | Timer/Charging cancellation transaction is **Proven**. Production ability assignment is design metadata. |
| Native side-effect suppression | Numeric and durable status ownership exist. Every remaining special consumer requires explicit provenance and suppression policy. |

## 7. DCL-owned runtime state

| State | Required lifecycle | Coverage and owner |
| --- | --- | --- |
| Hit/result caches | Keyed by generation and full action/target identity; cleared on hard boundaries. | Implemented in `06`. |
| Active-defense cadence | Repeated-Parry counters accumulate per weapon/limb across ActionInstances; Block spends once; both reset at the defender's own-turn boundary; checkpoint restore preserves the revisioned state. | Implemented offline; native invocation remains a binding gate. |
| Pending actions | Persist from confirmation to resolution/cancellation without “latest caster” guesses. | Proven for owned result/Interrupt families; canonical native admission is implemented offline without timestamp, latest-caster, or reusable-order-pointer inference. |
| Status durations | Count target turns, clear only owned bits, and neutralize a native counter only with complete producer ownership. | Implemented for runtime-owned DCL state durations and direct status fields. Native counter transfer is not active for any ordinary timed status without a current duration-pair manifest whose producers all have supported categories. Disable/Immobilize retain explicit physical Stun/Knockdown producers for ability ids `213/214`, but their shared native flags also have unresolved producers, so the generic native counters remain engine-owned. Doom and the system `Empty_32` row are excluded from the generic transaction. See `06`. |
| MP budget/trickle | Per-battle pool, own-turn edge, bounded writes, and reset. | Implemented offline; live policy coverage remains explicit. Overcast forecast exposes the declaration-time MP+HP split, ApprovedHPCap, projected pools, confirmation/legality, reason, and KO warning when the approved HP payment reaches zero. |
| Reaction cadence | Reserve, validate, commit, and deliver exactly once at the declared cadence. | Implemented offline for draw-free preflight before confirmed execution mutation/RNG, exact ordered candidates, once-per-window/native acceptance, per-effect-Strike completion, and one terminal window acknowledgement. Native candidate production and managed hook proof remain gated. |
| Movement transaction | Exact final destination and movement-complete boundary without mid-route mutation. | Position and route ownership are mapped; gameplay effects must wait for route completion. |
| Prepared states | One battle owner, exact persistent instance/use count, pre-application validation, and final-tile-only trigger revalidation. | Implemented offline for Overwatch, Cover/Bodyguard, Bulwark, and QuickLock. The state registry is the sole lifetime/use owner for trigger states and their controllers reconstruct from its current instance, so replacement, removal, KO cleanup, expiry, and checkpoint restore cannot retain duplicate mutable state. Overwatch application reserves and persists one ordered outer Action identity per authored trigger. An accepted final-tile batch binds the exact mover/tile/weapon and current legality verdicts, sorts candidates, blocks replay, consumes uses and the next reservation, and queues each firing declaration. The queue validates the exact family input before publication and retires only the exact settled native application; checkpoints preserve unused reservations but reject queued work, while unpublished tickets cancel on exact source/target identity loss without identity reuse. Every Overwatch payload also binds one native effect ability to its exact Action/revision; application and checkpoint restoration fail closed on disagreement. Cover/Bodyguard is limited to `PhysicalAttack`; tracked spell deliveries ignore physical cover. Physical planning and execution validate the source-owned protector/protected-unit link and exact attacker/declared-target/protector identities before mutation, preserve the declared target, and route the final target batch to the protector when current legality succeeds. Forecast/AI consumes no state or RNG; confirmed execution consumes or cancels the state immediately before physical execution RNG and publishes the removed state with the physical carrier. Quick validates the full persistent application before casting RNG or CT/controller mutation and rolls back both controller and exact CT grant if persistence fails. Granted-turn completion reserves an exact controller/state pair before target/source-turn expiry; disagreement fails without completing the turn, and the pair clears only after successful completion. Native finalizer invocation, prepared carrier application, expiry, timeline, and presentation bindings remain gated. |
| Battle lifecycle | Generation start/end, pointer reuse, unit removal, and settings reload. | Implemented offline with central reset. |
| AI evaluation | Publish authored transient numeric results without consuming execution state. | Numeric scoring and canonical Reaction activation evaluation are implemented offline. Reactions share exact AutomaticTrigger, SkillResponse, and one-reference 3d6 ActivationRoll projections with player forecast; native effect valuation remains family-specific. |

## 8. Completion criterion

The DCL runtime is technically complete only when all six information groups are owned and exposed:

1. action context;
2. action/effect metadata;
3. equipment metadata;
4. unit and map state;
5. outcome authority;
6. generation-safe DCL runtime state.

An offline implementation does not upgrade a native boundary to Proven. Each requirement keeps its
own confidence until the corresponding engine behavior is validated and promoted into its owning
document.

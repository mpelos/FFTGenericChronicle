# DCL Reaction Virtualization Scope Analysis

Generated by `tools/analyze_dcl_reaction_scope.py`.

## Inputs

- Executable: `D:\SteamLibrary\steamapps\common\FINAL FANTASY TACTICS - The Ivalice Chronicles\FFT_enhanced.exe`
- SHA-256: `841DD4048C9C33958156422CD96EE8D064F5BEB3C5F8A0E23A68AAF2BB87B282`
- PE image base: `0x140000000`
- Output: `work\1783988678-dcl-reaction-scope-analysis.md`

## Byte anchors

| Name | RVA | Result | Expected bytes | Meaning |
| --- | ---: | --- | --- | --- |
| `compute-action-result-entry` | `0x3099AC` | PASS | `48 89 5C 24 18 55 56 57` | guarded calc-entry hook shared by the DCL pipeline |
| `reaction-restore-epilogue` | `0x309FA1` | PASS | `48 83 C4 40 41 5F 41 5E` | guarded epilogue hook that restores the real Brave byte |
| `compute-action-result-return` | `0x309FB0` | PASS | `C3` | the sole linear-disassembly return in computeActionResult |
| `reaction-gate-r1` | `0x30BDEE` | PASS | `E8 75 D0 F6 FF` | real-code gate; exact id is held in r11d |
| `reaction-gate-r2` | `0x30BE44` | PASS | `E8 1F D0 F6 FF` | real-code gate; exact id is held in ebx |
| `reaction-gate-r3` | `0x30BE9A` | PASS | `E8 C9 CF F6 FF` | real-code gate; exact id is held in ebx |
| `reaction-gate-r4` | `0x30BEDA` | PASS | `E8 89 CF F6 FF` | real-code gate; exact id is held in r11d |
| `reaction-order-record-id` | `0x282F1E` | PASS | `66 89 77 02` | stages the exact Reaction id at order-record+2 before native reaction evaluation |
| `reaction-eval-global-id` | `0x282F22` | PASS | `66 89 35 C7 80 5E 01` | mirrors the same exact Reaction id into the reaction-evaluation global |
| `reaction-formula-global-read` | `0x3095EF` | PASS | `48 0F BF 0D F9 19 56 01` | native reaction formula dispatch reads the exact evaluation id |
| `reaction-incoming-source-read` | `0x282FCB` | PASS | `48 63 05 22 80 5E 01` | Counter staging reads the preserved incoming actor index from dword[0x14186AFF4] |
| `reaction-record-source-id` | `0x282FD5` | PASS | `88 47 0B` | Counter staging writes that incoming actor index into reaction order-record+0xB |

## Function-boundary check

- Linear disassembly: RVA `0x3099AC` through `0x309FB0`.
- Decoding reaches the expected final byte without a gap: **PASS**.
- Return instructions: `0x309FB0`.
- Sole guarded return at `0x309FB0`: **PASS**.

## Static interpretation

- **Strong:** `computeActionResult` has one linearly decoded return in the current executable.
  Installing the restore callback at the guarded epilogue covers every ordinary return path
  for explicitly marked VM-internal avoidance reactions.
- **Proven:** live reaction tests show that the engine consumes `unit+0x2B` as its native
  Brave-percent gate and that the reaction-evaluation global carries ids such as Mana Shield
  and Shirahadori while those reactions are evaluated.
- **Strong:** current-build reaction staging writes the exact id to `orderRecord+2` and the
  reaction-evaluation global before native formula dispatch reads it. Calc-entry already receives
  that order-record pointer, so VM chance virtualization can classify equipped or innate reactions
  by the actual evaluation id rather than by the defender's equipped slot.
- **Strong:** the four real-code roll gates retain the exact evaluated Reaction id in `r11d`
  or `ebx`. Their managed shim can replace only the chance while preserving native effects/RNG.
- **Strong:** native Counter staging reads `dword[0x14186AFF4]` as the incoming actor index,
  selects that unit, and writes its index and coordinates into the reaction order record.
  Reaction formulas can therefore receive the original source plus action type/id from the
  fresh per-target calc cache or the source unit's `+0x1A1/+0x1A2` order record.
- **Strong:** substituting `unit+0x2B` after the DCL hit/forecast callback and restoring it at
  the sole exit gives VM-internal avoidance such as Shirahadori an authored chance while
  leaving the permanent Brave trait, native reaction effect, and native RNG ownership unchanged.
- No persistent Brave write is required: a thread-local frame restores the byte at the guarded
  sole epilogue even when the evaluated Reaction is innate or derived.

## Restore epilogue

```text
00309FA1: 48 83 C4 40                  add rsp, 0x40
00309FA5: 41 5F                        pop r15
00309FA7: 41 5E                        pop r14
00309FA9: 41 5D                        pop r13
00309FAB: 41 5C                        pop r12
00309FAD: 5F                           pop rdi
00309FAE: 5E                           pop rsi
00309FAF: 5D                           pop rbp
00309FB0: C3                           ret
```

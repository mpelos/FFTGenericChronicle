# DCL native Counter Tackle analysis

Generated by `tools/analyze_dcl_counter_tackle.py`.

## Inputs

- Executable: `D:\SteamLibrary\steamapps\common\FINAL FANTASY TACTICS - The Ivalice Chronicles\FFT_enhanced.exe`
- SHA-256: `841DD4048C9C33958156422CD96EE8D064F5BEB3C5F8A0E23A68AAF2BB87B282`
- Ability catalog: `D:\Projects\FFTGenericChronicle\work\wotl_ability_action_baseline.csv`
- Active-data source: `D:\Projects\FFTGenericChronicle\work\override_ability.neuter.sqlite`
- Output: `work\1783999787-dcl-counter-tackle-analysis.md`

## Anchor verification

| RVA | Result | Meaning |
| ---: | --- | --- |
| `0x30B935` | PASS | loads reaction-set byte unit+0x96 |
| `0x30B958` | FAIL | tests unit+0x96 bit 0x08 for Counter Tackle |
| `0x30B95D` | PASS | rejects the disallowed incoming action class |
| `0x30B96A` | PASS | requires incoming result flag 0x10 |
| `0x30B977` | FAIL | selects exact reaction id 436 |
| `0x282F3D` | FAIL | routes id 436 to its explicit pass-2 branch |
| `0x283117` | PASS | selects payload ability 147 (Rush) |
| `0x28311D` | PASS | requests source-target validation |
| `0x283120` | PASS | selects typed order 0x0B |
| `0x283125` | PASS | enters the shared source-target order helper |
| `0x283294` | PASS | helper loads source index RVA 0x186AFF4 |
| `0x283321` | PASS | helper writes source index into the target descriptor |
| catalog `147` | PASS | `Rush` uses formula `0x37`: Dmg_(Rdm(1..Y)*PA) NS PE |
| override `147` | PASS | `Formula/X/Y=(-1, 1, 1)`; formula `-1` preserves native 0x37 |

## Native contract

Counter Tackle `436` is a real-code Courage reaction. Its pass-2 selector does not emit an
abstract shove: it emits a validated source-target typed order `(type=0x0B, payload=147)`,
where ability `147` is Rush. Formula `0x37` owns `random(1..Y) * PA` plus knockback. The
current neuter data preserves formula `0x37` and only reduces its X/Y magnitude, so the
knockback carrier remains intact while DCL damage can be rewritten downstream.

## DCL boundary

The final Squire Counter Tackle can retain native delivery. Its custom work is limited to
the trigger policy: landed melee weapon strike, adjacency, Courage chance, and any chosen
multi-hit cadence. The existing exact-id reaction filter can suppress nonqualifying native
evaluations without replacing Rush or synthesizing displacement. Live proof is bounded to
one accepted reaction confirming IVC knockback and source lifetime; no new effect mechanism
is required offline.

# DCL Malboro Spores transformation analysis

Generated by `tools/analyze_dcl_malboro_transformation.py`.

## Inputs

- Executable: `D:\SteamLibrary\steamapps\common\FINAL FANTASY TACTICS - The Ivalice Chronicles\FFT_enhanced.exe`
- SHA-256: `841DD4048C9C33958156422CD96EE8D064F5BEB3C5F8A0E23A68AAF2BB87B282`
- Ability catalog: `D:\Projects\FFTGenericChronicle\work\wotl_ability_action_baseline.csv`
- Output: `D:\Projects\FFTGenericChronicle\work\1784010934-dcl-malboro-transformation-analysis.md`

## Dispatch and catalog

- Formula `0x58` handler: `0x3090F0` (expected `0x3090F0`): **PASS**.
- Formula catalog contains only ability 329 Malboro Spores with no status payload: **PASS**.

## Byte anchors

| Name | RVA | Result | Meaning |
| --- | ---: | --- | --- |
| `transformation-eligibility` | `0x30913C` | PASS | requires a generic character id 0x80..0x82 and rejects special flags plus jobs 0x82..0x84 |
| `transformation-success-marker` | `0x309169` | PASS | stages result+0x12 bit 1 (word value 2) and marks a successful result |
| `transformation-ineligible-result` | `0x309174` | PASS | ineligible targets are converted to the native failure result rather than a status bundle |
| `transformation-state-branch` | `0x30A80A` | PASS | state apply recognizes result+0x12 bit 1 as the transformation transaction |
| `transformation-status-reset` | `0x30A857` | PASS | the native branch clears the target's staged/current status and effect state during transformation |

## Static interpretation

- **Strong:** formula `0x58` is a native unit-transformation transaction, not an InflictStatus
  bundle. Its only catalog member has no status payload, and success stages dedicated result
  bit `0x0002`.
- **Strong:** eligibility requires character id `0x80..0x82`, rejects `unit+0x05 & 0x04`,
  rejects nonzero `unit+0x1B4`, and rejects job ids `0x82..0x84`. The last range is a
  rejection: byte addition wraps those three values to `0..2`, which takes the failure branch.
- **Strong:** native state apply recognizes the dedicated bit and clears the unit's existing
  statuses/effects as part of transformation. The protected native continuation owns the job/
  appearance transition, so DCL should preserve it rather than synthesize status writes.
- **Live gate:** only campaign/battle persistence and the exact transformed job presentation
  remain observable integration checks; no custom DCL commit mechanism is required.

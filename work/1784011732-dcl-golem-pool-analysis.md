# DCL Golem pool analysis

Generated by `tools/analyze_dcl_golem_pool.py`.

## Inputs

- Executable: `D:\SteamLibrary\steamapps\common\FINAL FANTASY TACTICS - The Ivalice Chronicles\FFT_enhanced.exe`
- SHA-256: `841DD4048C9C33958156422CD96EE8D064F5BEB3C5F8A0E23A68AAF2BB87B282`
- Ability catalog: `D:\Projects\FFTGenericChronicle\work\wotl_ability_action_baseline.csv`
- Pool block: RVA `0x186B020` / VA `0x14186B020`
- Output: `D:\Projects\FFTGenericChronicle\work\1784011732-dcl-golem-pool-analysis.md`

## Dispatch and catalog

- Formula `0x14` handler: `0x307848` (expected `0x307848`): **PASS**.
- Formula catalog contains only ability 65 Golem: **PASS**.

## Byte anchors

| Name | RVA | Result | Meaning |
| --- | ---: | --- | --- |
| `formula-success-marker` | `0x3078D4` | PASS | a successful formula-0x14 calculation stages result+0x12=1 and result+0x27=1 |
| `pool-debit-selector` | `0x30A561` | PASS | a negative result+0x12 selects one of four pools from target unit+0x1EE high bits |
| `pool-saturating-debit` | `0x30A583` | PASS | the pool becomes max(pool-result+0x06,0), and the ordinary HP debit is skipped |
| `pool-initialize-from-max-hp` | `0x30A59E` | PASS | result bit 1 initializes the selected team pool from unit+0x32 MaxHP |
| `pool-state-import` | `0x30D855` | PASS | the native battle-state importer copies the packed four-pool qword into the static pool block |
| `pool-state-export` | `0x30DA63` | PASS | the matching exporter copies the packed four-pool qword out of the static pool block |

## Static interpretation

- **Strong:** the native state is four unsigned 16-bit pools packed at RVA `0x186B020`;
  both the initializer and consumer select an entry from a two-bit team/faction index.
- **Strong:** successful Golem application writes the selected unit's MaxHP into its pool.
  Golem is self-targeted in the catalog, so stock activation seeds the caster's team pool
  from the caster's MaxHP.
- **Strong:** an intercepted result is marked by a negative `result+0x12`. The state apply
  path subtracts the staged HP debit at `result+0x06`, clamps the pool to zero, and skips
  the ordinary HP debit entirely. An overkill hit therefore empties the remaining pool but
  does not spill residual damage through that same hit.
- **Strong:** the packed pool participates in the native battle-state import/export block.
  The DCL can reuse the native lifecycle and author activation magnitude at the mapped pool
  write; it does not need a separate managed barrier store.
- **Live gate:** confirm which incoming action families receive the negative interception
  marker and that recasting overwrites the active team pool exactly once.

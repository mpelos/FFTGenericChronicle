# DCL conditional-status producer analysis

Generated by `tools/analyze_dcl_status_conditional_producer.py`.

## Inputs

- Executable: `D:\SteamLibrary\steamapps\common\FINAL FANTASY TACTICS - The Ivalice Chronicles\FFT_enhanced.exe`
- SHA-256: `841DD4048C9C33958156422CD96EE8D064F5BEB3C5F8A0E23A68AAF2BB87B282`
- Ability catalog: `D:\Projects\FFTGenericChronicle\work\wotl_ability_action_baseline.csv`
- Output: `work\1784072795-dcl-status-postcalc-producer-analysis.md`

## Native anchors

| Anchor | RVA | Result | Meaning |
| --- | ---: | --- | --- |
| `calc-entry` | `0x3099AC` | PASS | universal per-action/target calculation entry |
| `outer-calc-call` | `0x281F0D` | PASS | ordinary target sweep calls calc |
| `post-calc-producer-host` | `0x281F12` | PASS | outer calculation has completed before loop advance |

## Inventory and implementation contract

- C# formula allowlist equals 12 conditional plus two performance families: **PASS**.
- Catalog rows owned by those families: `84` (expected `84`): **PASS**.
- Native-mode counts: `{'AllOrNothing': 72, 'Cancel': 7, 'Random': 5}`.
- Producer source enforces exact policy and complete packet ownership: **PASS**.
- Runtime source binds the post-calc callback, one cached plan, hit suppression, and native packet staging: **PASS**.
- Calc cache binds LT28 provenance and refuses the nested Rend overwrite: **PASS**.

## Boundary

The producer runs at outer-sweep return `0x281F12` and requires fire-time battle state `0x2A`.
Forecast calls return through `.trace` at `0xEF53F14` in state `0x19`, so they cannot consume
the execution 3d6 decision. Formula-`0x25` nested Attack rows are retained as evidence but do
not replace the outer action identity in the apply cache.
Formula `0x1C/0x1D` performance ticks use the same packet producer and exact catalog-derived
random-one ownership. Their native caster-Sleep eligibility check remains mandatory before
the managed contest; native cadence and cleanup remain live-gated.

For each configured `replaced-post-calc` action the loaded catalog supplies the complete native
packet bit set and native bundle mode. The producer clears/replaces every owned bit, stages the
result through the native five-byte add/remove transaction, and caches that exact decision for
pre-clamp reuse. A fully resisted or ineligible status-only action clears the packet and never
needs to reach pre-clamp. Forecast percentage and AI scoring still use native read-only behavior
until their authored-probability presentation layer is implemented.

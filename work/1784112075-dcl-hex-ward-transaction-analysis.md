# DCL Hex Ward accepted-Reaction transaction analysis

Generated by `tools/analyze_dcl_hex_ward_transaction.py`.

| Anchor | Result | Contract |
| --- | --- | --- |
| `disabled-default` | PASS | the composed mechanism is opt-in |
| `log-only-default` | PASS | the mechanism starts observe-only |
| `exact-carrier-rule` | PASS | one Caution rule owns carrier 443 |
| `hit-context` | PASS | only a known landed incoming action may reserve Hex Ward |
| `cadence-peek` | PASS | eligibility can be checked without early consumption |
| `no-early-consume` | PASS | the chance callback owns no cadence commit |
| `duplicate-gate` | PASS | replayed callbacks never restage |
| `dynamic-mailbox` | PASS | accepted defenders arm their own producer slot |
| `empty-slot` | PASS | the producer never overwrites a native candidate |
| `carrier-stage` | PASS | the dynamic producer stages exact carrier 443 |
| `bounded-stage` | PASS | live staging is capped |
| `retarget-contract` | PASS | the accepted order keeps 443 presentation and targets the attacker |
| `native-head-guard` | PASS | retargeting is guarded by the proven native order shape |
| `pass2-only` | PASS | effect delivery accepts only exact pass-2 carrier identity |
| `producer-owned-commit` | PASS | an unrelated 443 cannot trigger the managed effect |
| `source-commit` | PASS | the committed target must equal the reserved incoming source |
| `cadence-commit` | PASS | cadence is consumed only after pass-2 acceptance |
| `blind-immunity` | PASS | Blind respects the proven native immunity array |
| `blind-durable` | PASS | Blind writes the durable master bit |
| `blind-effective` | PASS | Blind writes the effective mirror |
| `blind-rollback` | PASS | a partial direct status write fails open with rollback |
| `brave-floor` | PASS | the alternate current-Brave effect has an authored floor |
| `duplicate-commit` | PASS | the reservation can commit only once |

## Contract

Carrier 443 is reserved only for a known landed incoming action after one managed Caution roll.
The pass-2 pre-selector consumes a per-defender request without overwriting native candidates;
the guarded materialization controller retargets the accepted order to the incoming source.
Only an exact producer-owned pass-2 commit consumes the attacker-action cadence and applies one
managed effect. Blind respects immunity and atomically updates durable/effective state; the
alternate current-Brave reduction is clamped to an authored floor. Replayed gate callbacks,
wrong sources, and duplicate commits are side-effect free.

Overall offline gate: **PASS**.

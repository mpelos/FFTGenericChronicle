# DCL Hex Ward carrier and target-inversion analysis

Generated by `tools/analyze_dcl_hex_ward.py`.

## Inputs

- Executable: `D:\SteamLibrary\steamapps\common\FINAL FANTASY TACTICS - The Ivalice Chronicles\FFT_enhanced.exe`
- SHA-256: `841DD4048C9C33958156422CD96EE8D064F5BEB3C5F8A0E23A68AAF2BB87B282`
- Override database: `D:\Projects\FFTGenericChronicle\work\override_ability.sqlite`
- Output: `D:\Projects\FFTGenericChronicle\work\1784112736-dcl-hex-ward-analysis.md`

## Anchor verification

| RVA | Result | Meaning |
| ---: | --- | --- |
| `0x30B935` | PASS | loads reaction-set byte unit+0x96 |
| `0x282E55` | PASS | loads incoming-source/exclusion index global RVA 0x186AFF4 |
| `0x282E9A` | PASS | reads the candidate exact id from unit+0x1CE |
| `0x282F1E` | PASS | writes candidate id to the staged order record |
| `0x282F22` | PASS | publishes the exact reaction id globally |
| `0x282F73` | PASS | enters the generic-reaction ability-flag route |
| `0x282F7A` | PASS | rejects only generic records whose flags contain 0x30 |
| `0x282F88` | PASS | generic route begins from the reactor's own tile X |
| `0x282FE6` | PASS | writes the selected target tile X |
| `0x2831BD` | PASS | builds the target list for the staged order |
| `0x206414` | PASS | mirrors selected id to actor+0x18C |
| `0x20641A` | PASS | mirrors selected id to executable actor+0x142 |
| `0x206421` | PASS | post-selector commit boundary with actor and target list live |
| `0x30B935..0x30BB40` | PASS | dispatcher never tests `unit+0x96 & 0x04`, the bit corresponding to blank id 443 |
| `0x67F745` | PASS | id 443 generic-selector flag is `0x06`; mask `0x30` is clear |

## Override-table boundary

- SQLite check: `ok`.
- Row span: count `368`, min `0`, max `367`.
- Id 443 excluded from `OverrideAbilityActionData`: **PASS**.

The sparse NXD formula surface ends below reaction id 443. Hex Ward cannot acquire a normal
action formula/status rider by authoring row 443 in that table.

## Native carrier contract

Blank reaction id `443` is a structurally valid generic pass-2 carrier: its selector flag
does not reject the record, and the selector can build a generic order for it. However,
the native reaction dispatcher never tests the corresponding equipped-reaction bit, so it
will not stage `unit+0x1CE = 443` by itself. If staged externally, the generic route targets
the reactor's own tile, not the incoming source.

The selector already owns the incoming-source index in global RVA `0x186AFF4`. After it
returns, the pass-2 commit boundary has the queued actor in `rbx`, ids in `actor+0x18C/+0x142`,
and the built target count/list at `actor+0x1A9/+0x1AA`. That is an execution-safe surface
for a guarded Hex Ward implementation to replace the target list with the source index.

## DCL implementation boundary

Hex Ward needs three custom operations: stage carrier 443 after a landed incoming hit and
its Caution roll; retarget the queued reaction to the source index; and deliver Blind or a
Brave decrease through the existing managed status/stat surfaces. Keeping `actor+0x18C = 443`
preserves Hex Ward presentation while the code mod owns the actual effect. The final write
must be idempotent and consume cadence only after pass-2 acceptance. The existing LT23 commit
probe is the bounded live gate for actor/target/source lifetime before enabling those writes.

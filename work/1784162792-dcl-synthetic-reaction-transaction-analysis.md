# DCL synthetic-Reaction transaction analysis

Generated by `tools/analyze_dcl_synthetic_reaction_transaction.py`.

| Anchor | Result | Contract |
| --- | --- | --- |
| `disabled-default` | PASS | the composed mechanism is opt-in |
| `log-only-default` | PASS | the mechanism starts observe-only |
| `configurable-owner` | PASS | the equipped owner identity is configurable |
| `configurable-delivery` | PASS | the native delivery identity is separate and configurable |
| `explicit-trigger` | PASS | the currently owned trigger family is explicit |
| `exact-carrier-rule` | PASS | one taxonomy rule owns the configured owner |
| `hit-context` | PASS | only a known landed incoming action may reserve the trigger |
| `cadence-peek` | PASS | eligibility can be checked without early consumption |
| `no-early-consume` | PASS | the chance callback owns no cadence commit |
| `successful-hit-owner` | PASS | the successful result path can own a missing native trigger |
| `synthetic-only-preclamp-entry` | PASS | a synthetic-only profile reaches the committed pre-clamp result owner |
| `synthetic-only-no-numeric-write` | PASS | a synthetic-only log gate observes the result without rewriting its native HP debit |
| `equipped-carrier-check` | PASS | only an exact equipped-carrier owner is evaluated |
| `survivor-check` | PASS | a lethal incoming result cannot reserve a reaction |
| `final-hp-check` | PASS | the pass-2 producer rejects a defender killed by a later strike |
| `final-ko-check` | PASS | the pass-2 producer rejects effective KO state |
| `committed-origin` | PASS | the reservation records successful-result provenance |
| `duplicate-gate` | PASS | replayed callbacks never restage |
| `dynamic-mailbox` | PASS | accepted defenders arm their own producer slot |
| `bounded-producer-loop` | PASS | one indexed loop handles every battle-table slot without exceeding the hook assembler budget |
| `producer-loop-cardinality` | PASS | the compact producer still covers all 21 battle-table slots |
| `empty-slot` | PASS | the producer never overwrites a native candidate |
| `delivery-stage` | PASS | the producer stages the configured native delivery carrier |
| `bounded-stage` | PASS | live staging is capped |
| `special-delivery-set` | PASS | delivery is restricted to carriers proven to traverse the guarded materialization path |
| `optional-rewrite-delivery-match` | PASS | an optional rewrite binds to delivery, never owner identity |
| `pass2-only` | PASS | commit accepts only the exact pass-2 delivery identity |
| `commit-unit-record` | PASS | the actor's captured +0x148 unit record, not the actor object, owns the reservation |
| `delivery-owned-state` | PASS | native materialization publishes successful delivery ownership |
| `delivery-handshake` | PASS | only an exact staged delivery observed at materialization advances the producer state |
| `producer-owned-commit` | PASS | blocked or unrelated native orders cannot consume cadence |
| `source-commit` | PASS | the committed source must equal the reserved incoming source |
| `cadence-commit` | PASS | cadence is consumed only after pass-2 acceptance |
| `native-delivery` | PASS | effect delivery belongs to the configured native carrier, not hard-coded managed writes |
| `duplicate-commit` | PASS | the reservation can commit only once |
| `forbid-dclhexward` | PASS | no job-specific settings remain in the runtime |
| `forbid-hex-ward` | PASS | no job-specific name remains in the runtime |
| `forbid-hex-ward` | PASS | the coordinator is job-agnostic |
| `forbid-hex-ward` | PASS | validation is job-agnostic |
| `forbid-synthetic_reaction_producer_{unitindex}_done` | PASS | the producer is never expanded into 21 assembler blocks |
| `forbid-processdclsyntheticreactioncommit(queuepass,-reactionid,-idsagree,-actor,-sourceindex);` | PASS | an actor object is never mistaken for its +0x148 unit record |

## Contract

An exact equipped carrier may reserve one configured taxonomy roll only after a successful
incoming result that the defender survives. The pass-2 pre-selector consumes a per-defender
request without overwriting native candidates and stages a separately configured delivery
carrier. The special-family materialization hook confirms that exact staged delivery before
its pass-2 commit consumes attacker-action cadence. Owner identity, native delivery identity,
and an optional order rewrite remain separate. The runtime contains no job name, fixed id,
or hard-coded status/stat effect.

Overall offline gate: **PASS**.

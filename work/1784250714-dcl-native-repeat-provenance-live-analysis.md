# DCL native-repeat provenance live analysis

Generated by `tools/analyze_dcl_native_repeat_provenance_live.py`.

- Log: `work\1784250564-lt41i-dcl-native-repeat-fixed-live.log`
- SHA-256: `B831D0B71EF2F5034035349DCCD31EE59E45DEABD9B97BB44DAB61F54DDEC7C0`
- Expected behavior: `fixed`
- Matching `0x2A -> 0x2F` transactions: `2` (minimum `1`)

| Calc n | Log lines | Exact action identity | Pre-clamp debit 0x2A / 0x2F | Applied delta 0x2A / 0x2F |
| --- | --- | --- | --- | --- |
| `17` / `18` | `257` / `264` | caster=16, type=0x01, ability=0, payload=124, target=0 | `189` / `189` | `1` / `1` |
| `31` / `32` | `343` / `351` | caster=16, type=0x01, ability=0, payload=18, target=6 | `210` / `126` | `1` / `1` |

## Conclusion

State `0x2F` is a second outer-sweep execution transaction with the same exact caster, action type,
ability, active-weapon payload, and target as the preceding state-`0x2A` transaction.
The two transactions are validated against their own native pre-clamp debits; equal native amounts
are not required because the engine can calculate a different debit for each repeated result.
In this fixed capture, both transactions independently enter the DCL, rewrite the native debit to one, and apply a one-point HP delta.

Overall live-evidence gate: **PASS**.

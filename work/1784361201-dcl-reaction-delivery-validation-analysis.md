# DCL Reaction native-delivery validation analysis

Generated by `tools/analyze_dcl_reaction_delivery_validation.py`.

## Inputs

- Executable: `D:\SteamLibrary\steamapps\common\FINAL FANTASY TACTICS - The Ivalice Chronicles\FFT_enhanced.exe`
- SHA-256: `841DD4048C9C33958156422CD96EE8D064F5BEB3C5F8A0E23A68AAF2BB87B282`
- Output: `D:\Projects\FFTGenericChronicle\work\1784361201-dcl-reaction-delivery-validation-analysis.md`

## Native byte anchors

| Name | RVA | Result | Expected bytes | Meaning |
| --- | ---: | --- | --- | --- |
| `typed-family-result` | `0x283019` | PASS | `85 C0 0F 84 33 01 00 00 E9 3A 01 00 00` | ids 435/436/437/442 require zero from the shared typed-order helper; nonzero restores |
| `typed-bonecrusher-result` | `0x283148` | PASS | `85 C0 75 14 44 88 67 01 C6 47 0A 05` | id 434 requires zero from its separate typed-order call; nonzero restores |
| `final-result` | `0x28315C` | PASS | `85 C0 74 5D BA 14 00 00 00` | final VM-owned validator must return zero; nonzero falls through to restore |
| `failure-restore` | `0x283160` | PASS | `BA 14 00 00 00 4C 8D 05 1C 7E 5E 01 44 8B CA 48 8B CF E8 95 31 DA FF` | failed delivery restores the previous 20-byte order and resumes scanning |
| `accepted-boundary` | `0x2831BD` | PASS | `48 8B CB E8 6F F0 FF FF` | only a delivery that passes its path-specific typed check and final validation reaches accepted materialization |

## Runtime source anchors

| Source | Result | Meaning |
| --- | --- | --- |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | disabled-by-default setting |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | startup installation |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | shared typed-family result hook binding |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | Bonecrusher result hook binding |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | safe final-result hook begins at the validator call |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | final hook relocates exactly call plus test |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | external typed-rejection restore target is outside the final hook |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | final result capture runs after the relocated call and test |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | only the final site passes an explicit relocation length |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | typed sites use Reloaded.Hooks automatic instruction sizing |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | private rejected mailbox state |
| `codemod\fftivc.generic.chronicle.codemod\Mod.cs` | PASS | bounded live evidence row |
| `codemod\fftivc.generic.chronicle.codemod\RuntimeSettingsValidator.cs` | PASS | fail-closed live composition |

## Transaction boundary

The selector clears `unit+0x1CE` before native special-delivery validation. A consumed
candidate is therefore not an accepted Reaction. The selector dispatches ids `435/436/437/442`
through the typed helper whose result is tested at `0x283019`; Bonecrusher `434` uses a
separate call/result at `0x283148`. Nonzero at either path-specific site restores the old
order at `0x283160`. A surviving order then reaches the common final validator; nonzero at
`0x28315C` also restores. Only a zero path-specific result followed by a zero final result can
reach `0x2831BD`, pass-2 commit, and state-`0x2C` delivery.
The final probe starts at `0x283157`, executes after exactly the seven-byte call+test span,
and leaves the external restore entry at `0x283160` unmodified.

The runtime probe records both results and changes only its private staged-mailbox byte to
`rejected` on an exact synthetic failure. Game order, candidate, cadence, and effects remain
native-owned. A later action may reserve a new request, while replay of the rejected action
cannot masquerade as an accepted commit.

Overall static gate: **PASS**.

# Canonical admission live-control checkpoint

## Intended proof

The next live gate is the mutation-free canonical admission sentinel described by
`1784545200-canonical-admission-live-runbook.md`. It proves that the guarded pre-calculation
callback observes the complete source, selected unit/tile, affected TargetBatch, native repeat
cardinality, and per-Strike weapon identity before any canonical result publication is enabled.

## Attempt boundary

The supported Windows computer-control runtime failed during its own kernel-asset initialization
with `The system cannot find the path specified`. The configured plugin entry file and the Windows
temporary directory both exist. The failure happened before the control API initialized, before
Reloaded-II or the game was opened, and before any input was sent.

No live DCL observation was produced. The admission hook therefore remains unproven live and its
default-disabled status is unchanged.

## Continued offline work

Investigation continues at the source-side native apply boundary, with special attention to lethal
HP payment and its KO/custom-state lifecycle. This does not require admission-hook publication or a
live battle to audit.
